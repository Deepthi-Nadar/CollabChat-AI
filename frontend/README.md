# CollabChat AI Frontend

React + Vite frontend for CollabChat AI.

## Setup

```powershell
npm install
copy .env.example .env
npm run dev
```

The app defaults to `http://127.0.0.1:5173` and expects the backend at `http://127.0.0.1:8000`.
